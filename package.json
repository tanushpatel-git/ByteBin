{
  "name": "bytebin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n gateway,llms -c cyan,magenta \"npm run dev --prefix api-gateway\" \"npm run dev --prefix llms-service\"",
    "start": "concurrently -k -n gateway,llms -c cyan,magenta \"npm start --prefix api-gateway\" \"npm start --prefix llms-service\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
