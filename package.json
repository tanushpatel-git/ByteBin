{
  "name": "bytebin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n gateway,llms,blog,file-service -c cyan,magenta,yellow,green \"npm run dev --prefix api-gateway\" \"npm run dev --prefix llms-service\" \"npm run dev --prefix blog-service\" \"npm run dev --prefix file-service\"",
    "start": "concurrently -k -n gateway,llms,blog,file-service -c cyan,magenta,yellow,green \"npm start --prefix api-gateway\" \"npm start --prefix llms-service\" \"npm start --prefix blog-service\" \"npm start --prefix file-service\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "dependencies": {
    "express": "^5.2.1",
    "nodemon": "^3.1.14"
  }
}
